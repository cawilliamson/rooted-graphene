#!/usr/bin/expect -f

set arg1 [lindex $argv 0]

set timeout -1
spawn $arg1
match_max 100000

# Loop to handle multiple passphrase prompts
expect {
    -re ".*pass ?phrase.*" {
        send -- "\r"
        exp_continue
    }
    eof
}
